<select name="{{ field_type.getInputName() }}" class="form-control select2">

    {% for value, option in field_type.options %}

        <option value="{{ value }}" {{ field_type.value == value ? 'selected' }}>{{ option }}</option>

    {% endfor %}

</select>
